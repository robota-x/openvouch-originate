{
  "name": "defi-hack",
  "private": true,
  "workspaces": ["apps/web", "apps/lending-api", "packages/*"],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": { "node": ">=22" }
}
